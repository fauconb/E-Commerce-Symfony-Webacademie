{% extends 'AdminBundle:layout:base-layout.html.twig' %}

{% block page_title %}Categorie{% endblock %}

{% block page_subtitle %}Création d'une catégorie{% endblock %}

{% block page_content %}
    <div class="container">
        <div class="raw">
            <div class="box box-primary">
                <div class="box-header with-border">
                    <h3 class="box-title">Crée une catégorie</h3>
                </div>
                {{ form_start(form) }}
                <div class="box-body">
                    <div class="form-group">
                        {{ form_label(form.name) }}
                        {{ form_widget(form.name, { 'attr': {'class': 'form-control', 'placeholder' : 'Entrez le nom de la catégorie'} }) }}
                    </div>
                    <div class="form-group">
                        {{ form_label(form.descMin) }}
                        {{ form_widget(form.descMin, { 'attr': {'class': 'form-control', 'placeholder' : 'Description simplifier'} }) }}
                    </div>
                    <div class="form-group">
                        {{ form_label(form.descLong) }}
                        {{ form_widget(form.descLong, { 'attr': {'class': 'form-control'} }) }}
                    </div>
                    <div class="form-group">
                        {{ form_label(form.parent) }}
                        {{ form_widget(form.parent, { 'attr': {'class': 'form-control'} }) }}
                    </div>
                </div>
                <div class="box-footer">
                    <button type="submit" class="btn btn-primary">Crée la catégorie</button>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock page_content %}

