{% extends 'base.html.twig' %}

{% block body %}
    <h1>SubCategorie</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subCategorie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ subCategorie.name }}</td>
            </tr>
            <tr>
                <th>Descmin</th>
                <td>{{ subCategorie.descMin }}</td>
            </tr>
            <tr>
                <th>Desclong</th>
                <td>{{ subCategorie.descLong }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('admin_subcategorie_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('admin_subcategorie_edit', { 'id': subCategorie.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
